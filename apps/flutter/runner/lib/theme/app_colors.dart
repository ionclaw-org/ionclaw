import 'package:flutter/material.dart';

// shared brand palette, kept in sync with the other ionclaw apps
class AppColors {
  const AppColors._();

  static const primary = Color(0xFF0A8DCF);
  static const header = Color(0xFF1A1A2E);
  static const screenBg = Color(0xFFF8F9FA);
  static const card = Color(0xFFFFFFFF);
  static const success = Color(0xFF2E7D32);
  static const runningDot = Color(0xFF4CAF50);
  static const danger = Color(0xFFC62828);
  static const muted = Color(0xFF9E9E9E);
}
